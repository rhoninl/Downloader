apiVersion: v1
kind: ConfigMap
metadata:
  name: usb-camera-dev-configmap
  namespace: deviceshifu
  annotations:
    workspace.id: "1157a0c4-8c1a-497a-945f-0569b6cded55"
    workspace.name: "workspacd1"
data:
  driverProperties: |
    driverSku: HTTP USB Camera Device
    driverImage: edgenesis/usb-camera:v0.0.1
  instructions: |
    instructions:
      video:
      capture:
      probe:
  telemetries: |
    telemetries:
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: deviceshifu-usb-camera-dev
    workspace.id: "1157a0c4-8c1a-497a-945f-0569b6cded55"
    workspace.name: "workspacd1"
  name: deviceshifu-usb-camera-dev
  namespace: deviceshifu
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deviceshifu-usb-camera-dev
  template:
    metadata:
      labels:
        app: deviceshifu-usb-camera-dev
        workspace.id: "1157a0c4-8c1a-497a-945f-0569b6cded55"
        workspace.name: "workspacd1"
    spec:
      containers:
        - image: edgehub/deviceshifu-http-http:nightly
          name: deviceshifu-usb-camera-dev
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: deviceshifu-config
              mountPath: "/etc/edgedevice/config"
              readOnly: true
          env:
            - name: EDGEDEVICE_NAME
              value: "edgedevice-usb-camera-dev"
            - name: EDGEDEVICE_NAMESPACE
              value: "devices"
        - image: edgenesis/usb-camera:v0.0.3
          securityContext:
            privileged: true
          name: camera-python
          env:
            - name: CAMERA_WIDTH
              value: "640"
            - name: CAMERA_HEIGHT
              value: "480"
            - name: VIDEONUMBER
              value: "0"
          volumeMounts:
            - name: video
              mountPath: "/dev"
          resources:
      volumes:
        - name: deviceshifu-config
          configMap:
            name: usb-camera-dev-configmap
        - name: video
          hostPath:
            path: /dev
      serviceAccountName: edgedevice-sa
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: deviceshifu-usb-camera-dev
    workspace.id: "1157a0c4-8c1a-497a-945f-0569b6cded55"
    workspace.name: "workspacd1"
  name: deviceshifu-usb-camera-dev
  namespace: deviceshifu
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: deviceshifu-usb-camera-dev
  type: ClusterIP
---
apiVersion: shifu.edgenesis.io/v1alpha1
kind: EdgeDevice
metadata:
  name: edgedevice-usb-camera-dev
  namespace: devices
  annotations:
    workspace.id: "1157a0c4-8c1a-497a-945f-0569b6cded55"
    workspace.name: "workspacd1"
spec:
  sku: "HTTP USB Camera Device"
  connection: Ethernet
  address: 0.0.0.0:5001
  protocol: HTTP
